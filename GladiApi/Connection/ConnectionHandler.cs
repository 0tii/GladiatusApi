using System.Net.Http;

namespace GladiApi.Connection
{
    /// <summary>
    /// Handles connecting to the gladiatus gameforge server.<br/>
    /// Returns a <see cref="CharacterSelector"/> of available characters for the user.<br/><br/>
    /// <br/><b>Abandoned since authenticating against GF servers is secured by captcha</b>
    /// </summary>
    public class ConnectionManager
    {
        //async static Task<CharacterSelector> Connect(string mail, string pw)
    }
}
